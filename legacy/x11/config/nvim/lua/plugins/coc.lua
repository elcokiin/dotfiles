
-- CoC (Conquer of Completion) plugin for Neovim
-- Provides IDE-like autocompletion and language server protocol support.

return {
  "neoclide/coc.nvim",
  branch = "release",
  config = function()
		-- custom config
  end
}
